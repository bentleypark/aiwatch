# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cloudflare
.wrangler
.dev.vars

# Playwright
test-results/
playwright-report/
blob-report/
.playwright-mcp/
playwright/.edge-bypass-state.json

# Env
.env
.env.local
.env.*.local

.dev.vars*
!.dev.vars.example
!.env.example
.vercel

# Discovery / B2B monetization (private — strategy, lead lists, outreach, competitive intel; NOT for public repo)
discovery/

# MemPalace
.mempalace/
entities.json
mempalace.yaml
.claude/mcp.json
.claude/scheduled_tasks.lock
.claude/hook-audit.jsonl

# Parallel-agent worktrees (Claude Code `--worktree`; see docs/reference/parallel-agents.md)
.claude/worktrees/

# Test coverage output (regenerated by vitest — never commit; #9c751b2 accidentally tracked it)
coverage/
worker/coverage/

# Local scratch handoffs (research/plan notes for a future session — the GitHub issue is the record; delete after build)
*-handoff.md
