⚙️ AIWatch non-negotiable workflow gates (#415) — re-check at the decision moment, not just at session start:
0. Start-of-work: the FIRST action on any new issue OR free-form code change is to invoke the `ship-issue` skill (loads the full runbook) — do this BEFORE investigating, branching, or coding, once per task. Then, BEFORE branching, run `git worktree list`: if it shows a worktree or a main-repo branch you didn't create (a concurrent session is active), work in a NEW worktree — never an in-place branch on the shared main repo (a concurrent session can switch it out from under you). Already being in your OWN worktree satisfies this. Skip only for pure Q&A / no-code-change turns.
1. Local verify (CLAUDE.md step 3.5): before committing, start the right dev server AND get the USER's explicit in-browser confirmation. Your own curl / Playwright checks do NOT satisfy this. "Tests pass" ≠ "feature verified" — STOP and wait for the user after requesting verification.
2. PR review (/pr-review-toolkit:review-pr) + Build/Test before every commit; auto-loop until a round is 0 Critical/Important.
3. Commit / push / PR / merge only after the user asks or confirms — a ready/MERGEABLE PR is not a cue to auto-merge.
4. No nag: never end a turn with "shall I proceed / merge / 진행할까요? / 다음 작업 진행할까요?". Report results and stop.
